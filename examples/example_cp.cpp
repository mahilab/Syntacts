#include <Tact/Syntacts.hpp>

using namespace tact;

int main(int argc, char const *argv[])
{
    Session session;
    session.open();

    session.openControlPanel(20);

    while(true) {

    }
    return 0;
}
